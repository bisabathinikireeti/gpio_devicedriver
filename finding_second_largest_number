#include<stdio.h>
void  second_large(int arr[],int n)
{
    int first_largest,second_largest;
    if(arr[0]>arr[1])
    {
        first_largest=arr[0];
        second_largest=arr[1];
    }
    else{
    first_largest=arr[1];
        second_largest=arr[0];
    }
    for(int i=0;i<n;i++)
    {
        if(arr[i]>first_largest)
        {
            second_largest=first_largest;
            first_largest=arr[i];
    
        }
        else if(arr[i]>second_largest && arr[i]!=first_largest)
        {
            second_largest=arr[i];
        }
    }
    printf("\n%d is second largest number",second_largest);
}

int main()
{
    printf("enter the number\n");
    int n;
    scanf("%d",&n);
    int arr[n];
    printf("enter the array numbers\n");
    for(int i=0;i<n;i++)
    {
        scanf("%d",&arr[i]);
    }
    for(int i=0;i<n;i++)
    {
        printf(" %d",arr[i]);
    }
    second_large(arr,sizeof(arr)/sizeof(arr[0]));
    return 0;
}
